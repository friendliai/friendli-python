# URLImageItemResponseFormat

The format of the generated image. One of `url(default)`, `raw`, `png`, `jpeg`, and `jpg`.

## Example Usage

```python
from friendli_core.models import URLImageItemResponseFormat
value: URLImageItemResponseFormat = "url"
```


## Values

- `"url"`
- `"raw"`
- `"png"`
- `"jpeg"`
- `"jpg"`
