# MessagesThinkingConfigType

Reasoning mode. `enabled` forces reasoning, `disabled` suppresses it, and `adaptive` lets the system decide.

## Example Usage

```python
from friendli_core.models import MessagesThinkingConfigType
value: MessagesThinkingConfigType = "enabled"
```


## Values

- `"enabled"`
- `"disabled"`
- `"adaptive"`
