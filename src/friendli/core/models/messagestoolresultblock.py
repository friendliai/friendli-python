"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from friendli.core.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from friendli.core.utils import validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, Dict, List, Literal, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict

MessagesToolResultBlockContentTypedDict = TypeAliasType(
    "MessagesToolResultBlockContentTypedDict", Union[str, List[Dict[str, Any]]]
)
"Tool result payload. Can be plain text, a list of objects, or null when no body is returned."
MessagesToolResultBlockContent = TypeAliasType(
    "MessagesToolResultBlockContent", Union[str, List[Dict[str, Any]]]
)
"Tool result payload. Can be plain text, a list of objects, or null when no body is returned."


class MessagesToolResultBlockTypedDict(TypedDict):
    tool_use_id: str
    "Identifier of the related `tool_use` block this result answers."
    type: Literal["tool_result"]
    "Content block type. Must be `tool_result`."
    content: NotRequired[Nullable[MessagesToolResultBlockContentTypedDict]]
    "Tool result payload. Can be plain text, a list of objects, or null when no body is returned."


class MessagesToolResultBlock(BaseModel):
    tool_use_id: str
    "Identifier of the related `tool_use` block this result answers."
    TYPE: Annotated[
        Annotated[
            Literal["tool_result"], AfterValidator(validate_const("tool_result"))
        ],
        pydantic.Field(alias="type"),
    ] = "tool_result"
    "Content block type. Must be `tool_result`."
    content: OptionalNullable[MessagesToolResultBlockContent] = UNSET
    "Tool result payload. Can be plain text, a list of objects, or null when no body is returned."

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["content"])
        nullable_fields = set(["content"])
        serialized = handler(self)
        m = {}
        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields and self.__pydantic_fields_set__.intersection({n})
            )
            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val
        return m


try:
    MessagesToolResultBlock.model_rebuild()
except NameError:
    pass
