"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from friendli.core.types import BaseModel
from friendli.core.utils import validate_const
import pydantic
from pydantic.functional_validators import AfterValidator
from typing import Literal
from typing_extensions import Annotated, TypedDict


class MessagesBase64ImageSourceTypedDict(TypedDict):
    data: str
    "Base64-encoded image bytes (without data URL prefix)."
    media_type: str
    "MIME type, for example `image/png`."
    type: Literal["base64"]
    "Image source type. Must be `base64`."


class MessagesBase64ImageSource(BaseModel):
    data: str
    "Base64-encoded image bytes (without data URL prefix)."
    media_type: str
    "MIME type, for example `image/png`."
    TYPE: Annotated[
        Annotated[Literal["base64"], AfterValidator(validate_const("base64"))],
        pydantic.Field(alias="type"),
    ] = "base64"
    "Image source type. Must be `base64`."


try:
    MessagesBase64ImageSource.model_rebuild()
except NameError:
    pass
