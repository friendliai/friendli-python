"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from friendli.core.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Literal
from typing_extensions import NotRequired, TypedDict

MessagesToolChoiceType = Literal["auto", "any", "tool", "none"]
"Tool-calling mode. `auto` lets the model decide, `any` requires at least one tool call, `tool` forces a named tool, and `none` disables tool calls."


class MessagesToolChoiceTypedDict(TypedDict):
    type: MessagesToolChoiceType
    "Tool-calling mode. `auto` lets the model decide, `any` requires at least one tool call, `tool` forces a named tool, and `none` disables tool calls."
    name: NotRequired[Nullable[str]]
    "Tool name to force when `type=tool`. Ignored for other `type` values."
    disable_parallel_tool_use: NotRequired[Nullable[bool]]
    "If true, restricts the model to at most one tool call at a time. Must not be provided when `type=none`."


class MessagesToolChoice(BaseModel):
    type: MessagesToolChoiceType
    "Tool-calling mode. `auto` lets the model decide, `any` requires at least one tool call, `tool` forces a named tool, and `none` disables tool calls."
    name: OptionalNullable[str] = UNSET
    "Tool name to force when `type=tool`. Ignored for other `type` values."
    disable_parallel_tool_use: OptionalNullable[bool] = UNSET
    "If true, restricts the model to at most one tool call at a time. Must not be provided when `type=none`."

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["name", "disable_parallel_tool_use"])
        nullable_fields = set(["name", "disable_parallel_tool_use"])
        serialized = handler(self)
        m = {}
        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields and self.__pydantic_fields_set__.intersection({n})
            )
            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val
        return m
