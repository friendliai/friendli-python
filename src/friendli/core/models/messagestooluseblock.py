"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from friendli.core.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from friendli.core.utils import validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, Dict, Literal
from typing_extensions import Annotated, NotRequired, TypedDict


class MessagesToolUseBlockTypedDict(TypedDict):
    name: str
    "Name of the tool to invoke."
    input: Dict[str, Any]
    "Tool input argument object."
    type: Literal["tool_use"]
    "Content block type. Must be `tool_use`."
    id: NotRequired[Nullable[str]]
    "Optional tool call identifier. Use this value to link follow-up `tool_result` blocks."


class MessagesToolUseBlock(BaseModel):
    name: str
    "Name of the tool to invoke."
    input: Dict[str, Any]
    "Tool input argument object."
    TYPE: Annotated[
        Annotated[Literal["tool_use"], AfterValidator(validate_const("tool_use"))],
        pydantic.Field(alias="type"),
    ] = "tool_use"
    "Content block type. Must be `tool_use`."
    id: OptionalNullable[str] = UNSET
    "Optional tool call identifier. Use this value to link follow-up `tool_result` blocks."

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id"])
        nullable_fields = set(["id"])
        serialized = handler(self)
        m = {}
        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields and self.__pydantic_fields_set__.intersection({n})
            )
            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val
        return m


try:
    MessagesToolUseBlock.model_rebuild()
except NameError:
    pass
